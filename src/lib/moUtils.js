/**
 * =============================================================================
 * moUtils.js — общие хелперы для названий МО
 * =============================================================================
 *
 * truncateName раньше дублировался в dashboardPhase1.js и dashboardPhase2.js
 * (байт-идентичен). Вынесен сюда.
 *
 * ПОСЛЕ ПРАВОК: npm run build → npm test (tests/moUtils.test.js)
 * =============================================================================
 */

/** Обрезать длинное название МО до max символов, добавив многоточие */
export function truncateName(name, max) {
  const n = String(name || '');
  return n.length > max ? n.slice(0, max - 1) + '…' : n;
}
