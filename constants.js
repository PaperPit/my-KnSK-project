/**
 * Единые константы расчётов дашборда КнСК
 */
var PLAN_WEEKLY = 3333;
var PLAN_YEAR = 160000;
var PLAN_THRESHOLD = 70;

if (typeof window !== 'undefined') {
  window.PLAN_WEEKLY = PLAN_WEEKLY;
  window.PLAN_YEAR = PLAN_YEAR;
  window.PLAN_THRESHOLD = PLAN_THRESHOLD;
}
